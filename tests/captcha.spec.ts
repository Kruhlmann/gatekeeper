import { expect } from "chai";
import "mocha";

describe("CAPTCHAs", () => {
    describe("Hit chance", () => {
        describe("Orc", () => {
            it("should return true", () => {
                expect(1).to.equal(1)
            });
        });
        describe("Orc", () => {
            it("should return true", () => {
                expect(1).to.equal(1)
            });
        });
    });
});

